import axios from 'axios';

export default async function log(message: string) {
  // Placeholder: Send log message to Huddl server
  console.log(`Log command not yet implemented. Message: ${message}`);
}
