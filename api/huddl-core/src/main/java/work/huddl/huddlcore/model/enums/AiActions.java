package work.huddl.huddlcore.model.enums;

/**
 * Enum representing different AI actions that can be performed within the Huddl
 * system.
 * Each action corresponds to a specific task or function that the AI can assist
 * with.
 */
public enum AiActions {

	SUMMARIZE,
	CORRECT,
	SUGGEST,
	PLAN,
	JOURNAL,
	REVIEW,
	REFLECT,
	PRIORITIZE,
}

/**
 * The reason AI Actions are defined in a seperate enum from Node Actions is:
 * ???
 */
